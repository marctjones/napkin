# amendments: Connecticut's operations on IRC tables

Empty because no IRC table is loaded, so there is nothing for an operation to act on. When you fill
`layers/irc-2021/tables/`, add one `<table>.json` here per table. Connecticut's own amendments that
touch header tables are recorded, verbatim and cited, in `../ct-overlay-data.json`
(Tables R602.7(1) footnote e and R602.7(3) footnote b).
